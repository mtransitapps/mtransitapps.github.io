name: Merge RSS Feed PR

on:
  schedule:
    # Run on the 2nd at 5:59 PM Eastern Time (America/Montreal), 1 min before RSS publish at 6 PM
    # DST months (March-November): 5:59 PM EDT = 21:59 UTC
    - cron: '59 21 2 3-11 *'  # March through November (EDT)
    # Standard time months (December-February): 5:59 PM EST = 22:59 UTC
    - cron: '59 22 2 1-2,12 *'  # January, February, December (EST)
  workflow_dispatch:  # Allow manual triggering for testing

permissions:
  contents: write
  pull-requests: write

jobs:
  merge-rss-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Find and merge PR with lastmonth branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Find open PRs with branch name containing "lastmonth"
          echo "Searching for open PRs with 'lastmonth' in branch name..."

          # Get list of open PRs
          prs=$(gh pr list --state open --json number,headRefName,title)
          echo "Open PRs: $prs"

          # Filter PRs with "lastmonth" in branch name
          pr_number=$(echo "$prs" | jq -r '.[] | select(.headRefName | contains("lastmonth")) | .number' | head -1)

          if [ -z "$pr_number" ]; then
            echo "No open PR found with 'lastmonth' in branch name"
            exit 0
          fi

          echo "Found PR #$pr_number with lastmonth branch"

          # Get PR details
          pr_info=$(gh pr view "$pr_number" --json headRefName,title,mergeable)
          echo "PR Info: $pr_info"

          branch_name=$(echo "$pr_info" | jq -r '.headRefName')
          pr_title=$(echo "$pr_info" | jq -r '.title')
          mergeable=$(echo "$pr_info" | jq -r '.mergeable')

          echo "Branch: $branch_name"
          echo "Title: $pr_title"
          echo "Mergeable: $mergeable"

          # Check if PR is mergeable
          if [ "$mergeable" != "MERGEABLE" ]; then
            echo "⚠️ PR #$pr_number is not mergeable (status: $mergeable)"
            echo "Please check for conflicts or other issues"
            exit 1
          fi

          # Merge the PR
          echo "Merging PR #$pr_number..."
          gh pr merge "$pr_number" --squash --auto --delete-branch

          echo "✅ Successfully merged PR #$pr_number"
